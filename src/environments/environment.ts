export const environment = {
  production: false,
  /**
   * Empty in dev: the browser calls the same origin as `ng serve`, and `proxy.conf.json`
   * forwards `/api` to Spring Boot at http://localhost:8080.
   */
  apiUrl: '',
};
